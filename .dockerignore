# Build context is the repository root, so without this every image build ships the .git history
# and every target/ directory to the daemon — including the 12 MB corpus fixture and the fat jars.
target/
**/target/
.git/
.idea/
*.iml
.DS_Store

# The fixture is a test resource. Nothing inside a service image reads it, and it is the single
# largest thing in the repo.
tools/corpus-generator/fixtures/
